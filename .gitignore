# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Server/runtime secrets. Keep .env.example as the committed template.
.env
.env.*
!.env.example

# Recovered original website archive. Keep on disk as read-only source/reference.
# Do not commit or deploy this tree with the production application.
legacy-crs-site/

# Local-only media working copies and recut previews. Never deploy.
local-preview/

# Local Windows thumbnail caches
Thumbs.db
ehthumbs.db

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
